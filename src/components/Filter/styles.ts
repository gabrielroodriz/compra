import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
    gap: 5,
    padding: 4,
  },
  title: {
    fontSize: 12,
    fontWeight: 600,
  },
});
